# SQL Warehouse
# Use for: SQL query execution
# OBO scope (per-user authorization): sql.warehouses — see SKILL.md "OBO" section.

- name: 'my_warehouse'
  sql_warehouse:
    sql_warehouse_id: 'abc123def456'
    permission: 'CAN_USE'
